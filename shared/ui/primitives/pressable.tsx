// `pressableVariants` (as classes CVA) não faz parte da API pública do pacote —
// mesma decisão documentada em `badge.tsx`. Quem precisa de outra aparência usa
// as props do componente, não as classes por baixo dele.
export { Pressable } from "@system-automation/design-system";
