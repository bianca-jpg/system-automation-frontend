// Botão "Carregar mais" do design system (`ActionButton` secundário: largura
// cheia no mobile, ~10rem a partir de `sm`). É o padrão de paginação do DS — a
// contrapartida numerada (`shared/ui/composite/PaginationControls.tsx`) só é do
// app porque paginação por página NÃO vem de lá.
export { LoadMoreButton } from "@system-automation/design-system";
