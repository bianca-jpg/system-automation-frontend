// `PageLoader` não está no barrel raiz do design-system — só no subpath
// `@system-automation/design-system/components/page-loader` (ver "exports" do package.json).
export { PageLoader } from '@system-automation/design-system/components/page-loader';
