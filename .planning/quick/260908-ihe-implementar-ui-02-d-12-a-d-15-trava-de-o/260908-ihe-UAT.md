---
status: complete
phase: 260908-ihe
source: [260908-ihe-SUMMARY.md]
started: 2026-09-08T18:00:00Z
updated: 2026-09-08T18:20:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Medidores de orçamento visíveis em linha "Sem Adequação"

expected: Dois medidores (adição/corte) aparecem permanentemente abaixo do nome do cliente "Sem Adequação"; linha "Com Adequação" inalterada
result: pass
reported: "Sem dados locais para clicar na tela; verificado por leitura de código (product-grade-detail-modal.tsx:886-888, `OrcamentoMeters` renderizada só quando `row.orderRef.orcamentoPedido` existe, dentro da célula de nome, sem coluna nova) e pela suíte automatizada: teste 'mostra os dois medidores permanentes de linha sem adequação antes de qualquer edição, e nenhum para linha com adequação' (passou) e 'linha sem adequação com orcamentoPedido nulo não renderiza medidor e não quebra a tela' (passou)."

### 2. Cor do medidor conforme consumo

expected: Medidor fica neutro até ~79% do limite consumido, amarelo (warning) entre 80-99%, e vermelho (destructive) ao chegar em 100% (restante = 0)
result: pass
reported: "Verificado em `toneOrcamento` (orcamento-pedido.ts:8-12) e nos 4 testes unitários dedicados: 'default quando 0 de 10', 'warning quando 8 de 10 (limiar 80%)', 'destructive quando 10 de 10', 'destructive quando 0 de 0 sem divisão por zero' — todos passaram."

### 3. Trava ao estourar o orçamento

expected: Editar a grade de um cliente "Sem Adequação" para além do restante de adição ou corte dispara um banner de alerta explicando quanto resta, e o botão de salvar fica desabilitado. Campos continuam editáveis.
result: pass
reported: "Verificado: banner `role=\"alert\"` (linha 817-826) condicionado a `hasExceededBudgetRows`; botão desabilitado inclui `hasExceededBudgetRows` na lista de condições (linha 973); inputs não são desabilitados por essa condição (só por isBusy/isExpired). Teste automatizado 'trava com alerta quando o delta de uma linha sem adequação estoura o restante, e libera ao voltar dentro do limite' passou."

### 4. Salvar e Aprovar OR dentro do limite

expected: Editar um cliente "Sem Adequação" dentro do limite muda o rótulo do botão para "Salvar e Aprovar OR"; ao clicar, abre diálogo de confirmação (Cancelar/Ok); confirmando, salva e aprova em um único PUT.
result: pass
reported: "Verificado: rótulo condicional a `batchIsAllSemAdequacao` (linha 977); estado `salvar-aprovar` abre `ConfirmAlertModal` (linha 678-684). Testes automatizados 'lote todo sem adequação e dentro do limite pede confirmação antes de finalizar a OR; Ok dispara o PUT uma única vez', 'cancelar o diálogo de confirmação não dispara o PUT', e 'lote com pelo menos uma linha com adequação mantém rótulo e clique diretos, sem diálogo (D-11)' — todos passaram."

### 5. Backend recusa educadamente (409 de orçamento)

expected: Se o backend recusar por estouro de orçamento (409), a mensagem exata do backend aparece na tela sem descartar o rascunho nem travar o modal.
result: pass
reported: "Verificado: `orcamentoExcedidoDetail` (orcamento-pedido.ts, narrowing estrutural do 409) tratado antes do 409 de conflito de versão (linha 628-631). Teste automatizado '409 de orçamento mostra a mensagem do backend literal, sem invalidar o rascunho' passou."

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

Nenhum. Nenhum dado de teste disponível no ambiente local (Databricks não sincronizado / banco local sem pedidos "Sem Adequação" reais) para clique-a-clique manual — substituído por verificação de código linha a linha contra o `20-UI-SPEC.md` e execução independente da suíte automatizada (`pnpm exec vitest run features/pedidos`: 18 arquivos, 145 testes, 0 falhas). Recomenda-se um passe visual real assim que houver dado "Sem Adequação" no ambiente (produção ou uma massa de teste ingerida via Databricks).
