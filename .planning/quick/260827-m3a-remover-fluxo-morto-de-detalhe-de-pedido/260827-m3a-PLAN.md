---
phase: 260827-m3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/pedidos/ui/order-detail-modal.tsx
  - features/pedidos/ui/order-detail-modal.test.tsx
  - features/pedidos/model/app-data-provider.tsx
  - widgets/pedido-dashboard/ui/orders-list.tsx
  - widgets/pedido-dashboard/ui/orders-list.test.tsx
  - widgets/pedido-dashboard/ui/orders-list.pagination.test.tsx
  - widgets/pedido-dashboard/ui/orders-list.processing.test.tsx
  - widgets/pedido-dashboard/ui/orders-list-skeleton.test.tsx
  - app/(app)/pedidos/page.tsx
  - app/(app)/pedidos/page.test.tsx
  - app/(app)/historico/page.tsx
  - app/(app)/alertas/page.tsx
autonomous: true
requirements: [QUICK-260827-M3A]

must_haves:
  truths:
    - "As telas de Pedidos, Histórico e Alertas continuam renderizando a lista de OR exatamente como antes"
    - "O clique na linha continua abrindo o ProductGradeDetailModal (fluxo vivo), sem regressão"
    - "Nenhum arquivo de código-fonte referencia mais o modal morto nem sua prop de abertura"
    - "`pnpm typecheck` e `pnpm lint` passam sem erro novo"
    - "`pnpm test` não introduz nenhuma falha além das já instáveis no baseline"
  artifacts:
    - path: "widgets/pedido-dashboard/ui/orders-list.tsx"
      provides: "OrdersList sem prop morta e sem o bloco de comentário PENDÊNCIA CONHECIDA"
    - path: "features/pedidos/model/app-data-provider.tsx"
      provides: "AppDataContextType sem o par de estado que só alimentava o modal morto"
    - path: "app/(app)/pedidos/page.tsx"
      provides: "Página de pedidos sem import/render do modal morto"
    - path: "app/(app)/historico/page.tsx"
      provides: "Página de histórico sem import/render do modal morto"
  key_links:
    - from: "widgets/pedido-dashboard/ui/orders-list.tsx"
      to: "features/pedidos/ui/modals/product-grade-detail-modal.tsx"
      via: "onRowClick={setSelectedGroupedProduct} — o fluxo de detalhe VIVO, que deve continuar intacto"
      pattern: "ProductGradeDetailModal"
---

<!-- planner-discipline-allow: onViewOrder -->
<!-- planner-discipline-allow: OrderDetailModal -->
<!-- planner-discipline-allow: selectedOrderDetails -->
<!-- planner-discipline-allow: setSelectedOrderDetails -->

<objective>
Remover o fluxo morto de detalhe de pedido (`OrderDetailModal` + a prop `onViewOrder` que o abriria + o par de estado `selectedOrderDetails`/`setSelectedOrderDetails` no `AppDataProvider`).

Purpose: o componente foi construído e testado, mas nunca é acionado — `OrdersList` nunca chama `onViewOrder`. O comentário "PENDÊNCIA CONHECIDA" em `orders-list.tsx` condicionava a religação à "normalização da OR por produto", que **já aconteceu** (backend `fb589d5` de 2026-08-04 e `02ee402` de 2026-08-26, este último apagando os endpoints por pedido). O substituto — `ProductGradeDetailModal` — já está vivo e consumindo o contrato atual produto+cliente. A pendência se resolve por remoção, não por reativação.

Output: ~270 linhas de componente morto + seu teste apagados; 12 arquivos limpos de props/estado órfãos; zero mudança de comportamento visível ao usuário.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Investigação já feita e verificada por leitura direta do código nesta sessão (2026-08-27) — **não re-derivar, apenas executar**. Todos os números de linha abaixo são do estado atual do repositório.

**O que é morto:**
- `features/pedidos/ui/order-detail-modal.tsx` (270 linhas) e `features/pedidos/ui/order-detail-modal.test.tsx` — renderizados em `pedidos/page.tsx` e `historico/page.tsx`, mas o único gatilho que os abriria (`onViewOrder`, prop obrigatória de `OrdersList`) nunca é invocado dentro de `widgets/pedido-dashboard/ui/orders-list.tsx`.
- Em `alertas/page.tsx` (linha 44) a prop já é passada como `() => {}` com o comentário "Sem visualização de modal nesta aba".
- Nenhum teste verifica comportamento real do modal a partir da lista: todos os arquivos que passam a prop usam `noop`/`() => {}` só para satisfazer o tipo obrigatório, e `app/(app)/pedidos/page.test.tsx` mocka o módulo inteiro como `() => null`.

**O que é vivo e NÃO se toca:** `features/pedidos/ui/modals/product-grade-detail-modal.tsx` (837 linhas), já importado em `orders-list.tsx`, já aberto de verdade pelo clique de linha (`onRowClick={setSelectedGroupedProduct}`, ~1651/1765/1969) e por handlers explícitos (~1274/1295).

**Três detalhes que a investigação inicial não enumerou e que a Task 2 precisa cobrir:**
1. `app-data-provider.tsx:127` — há uma chamada `setSelectedOrderDetails(null)` dentro do effect de troca de token (o "zera tudo quando o token muda"). Ela some junto; o resto do effect e o array de deps `[accessToken, resetResumo, queryClient]` permanecem intactos.
2. `app-data-provider.tsx:229` — o estado também aparece no array de dependências do `useMemo` do `contextValue` (além da linha 215 dentro do objeto). As duas ocorrências saem.
3. `app-data-provider.tsx:19` — `import type { EvolucaoFaturamento, Order } from '@/shared/types/models'` — `Order` é usado **somente** nas 3 linhas que serão removidas (37, 38, 112). O import precisa virar só `EvolucaoFaturamento`, senão o lint quebra por import não usado.

**Fora de escopo (não fazer):** o refactor maior de `OrdersList` (extrair estado de mutação/filtro/ordenação para hooks) e qualquer alteração no `ProductGradeDetailModal`.

**Baseline de testes (memória do projeto):** ~5 testes do vitest completo já falham sozinhos antes desta mudança. Não culpar a remoção sem comparar contra o baseline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover a prop morta de OrdersList e de todos os seus call sites</name>
  <files>widgets/pedido-dashboard/ui/orders-list.tsx, app/(app)/pedidos/page.tsx, app/(app)/historico/page.tsx, app/(app)/alertas/page.tsx, widgets/pedido-dashboard/ui/orders-list.test.tsx, widgets/pedido-dashboard/ui/orders-list.pagination.test.tsx, widgets/pedido-dashboard/ui/orders-list.processing.test.tsx, widgets/pedido-dashboard/ui/orders-list-skeleton.test.tsx</files>
  <action>
Em `widgets/pedido-dashboard/ui/orders-list.tsx`, remover três coisas:
(a) a linha 286 da interface `OrdersListProps` que declara a prop `onViewOrder: (order: Order) => void;`;
(b) no destructuring do componente (linhas 404-405), a linha do parâmetro **e** a linha `// eslint-disable-next-line @typescript-eslint/no-unused-vars -- ver nota abaixo` imediatamente acima dela — o disable existia só por causa desse parâmetro;
(c) o bloco de comentário "PENDÊNCIA CONHECIDA (normalização da OR por produto)" nas linhas 413-418, inteiro. A pendência está sendo resolvida por remoção, não por reativação — o comentário não deve ser reescrito para dizer outra coisa, deve sumir. Preservar a linha em branco e o comentário seguinte ("Cada fila tem busca e cursor próprios...") intactos.

Não tocar em `Order` no import de tipos deste arquivo: o tipo continua em uso na prop `orders?: Order[]` e em outros pontos.

Depois, remover a linha `onViewOrder={...}` de cada call site de `<OrdersList>`:
- `app/(app)/pedidos/page.tsx` linha 47 (passa `setSelectedOrderDetails`);
- `app/(app)/historico/page.tsx` linha 47 (idem);
- `app/(app)/alertas/page.tsx` linha 44 — remover a linha inteira, incluindo o comentário de fim de linha "Sem visualização de modal nesta aba", que perde o referente;
- `widgets/pedido-dashboard/ui/orders-list.test.tsx` linhas 104, 151, 177, 199, 231, 503;
- `widgets/pedido-dashboard/ui/orders-list.pagination.test.tsx` linhas 130, 167, 196, 298, 359, 401, 447, 518, 566 (JSX) **e** a linha 228, que é uma propriedade de objeto (`onViewOrder: () => {},`) numa factory de props, não JSX — remover também;
- `widgets/pedido-dashboard/ui/orders-list.processing.test.tsx` linha 115;
- `widgets/pedido-dashboard/ui/orders-list-skeleton.test.tsx` linha 68.

Atenção: o helper `const noop = () => {}` em `orders-list.test.tsx` (linha 49) e em `orders-list-skeleton.test.tsx` (linha 57) continua sendo usado por `setCommunications` e `setAlerts` — **não** removê-lo.

Esta task deixa `selectedOrderDetails`/`setSelectedOrderDetails` ainda existindo no provider e nas páginas (apenas sem consumidor na lista) — isso é intencional para que o typecheck feche verde ao fim da task; a Task 2 termina a limpeza.
  </action>
  <verify>
    <automated>pnpm typecheck && test "$(grep -rn 'onViewOrder' app features widgets shared entities lib 2>/dev/null | wc -l)" -eq 0</automated>
  </verify>
  <done>`pnpm typecheck` passa; nenhuma ocorrência da prop resta em `app/`, `features/`, `widgets/`, `shared/`, `entities/` ou `lib/`; o bloco de comentário PENDÊNCIA CONHECIDA e o `eslint-disable` associado não existem mais; `noop` segue definido e usado nos dois arquivos de teste.</done>
</task>

<task type="auto">
  <name>Task 2: Apagar o modal morto e o estado de contexto que só o alimentava</name>
  <files>features/pedidos/ui/order-detail-modal.tsx, features/pedidos/ui/order-detail-modal.test.tsx, features/pedidos/model/app-data-provider.tsx, app/(app)/pedidos/page.tsx, app/(app)/pedidos/page.test.tsx, app/(app)/historico/page.tsx</files>
  <action>
Apagar os dois arquivos `features/pedidos/ui/order-detail-modal.tsx` e `features/pedidos/ui/order-detail-modal.test.tsx` (usar `git rm`, não deixar arquivo órfão no working tree).

Em `app/(app)/pedidos/page.tsx`: remover o import do modal (linha 6), o bloco JSX `<OrderDetailModal order={...} onClose={...} />` (linhas 59-62) e as duas linhas de destructuring `selectedOrderDetails,` / `setSelectedOrderDetails,` do `useAppData()` (linhas 17-18). O restante do destructuring (`selectedChannel`, `selectedMonthSimulated`, `resumo`, `erroDeCarga`, `tentarNovamenteRecursos`, `refetchPedidosResumo`) fica.

Em `app/(app)/historico/page.tsx`: mesma operação — import (linha 6), bloco JSX (linhas 53-56), destructuring (linhas 21-22). Sobram `selectedChannel` e `selectedMonthSimulated`.

Em `app/(app)/pedidos/page.test.tsx`: remover o bloco `vi.mock('@/features/pedidos/ui/order-detail-modal', ...)` (linhas 22-24) — obrigatório, porque `vi.mock` sobre um módulo inexistente falha na resolução — e remover as duas chaves do objeto `baseAppData` (linhas 40-41). Nenhuma asserção do arquivo inspeciona as props capturadas de forma exata (não há `toEqual`/`toMatchObject` sobre elas), então nada mais precisa mudar ali.

Em `features/pedidos/model/app-data-provider.tsx`, remover as **cinco** ocorrências:
1. linhas 37-38 — os dois campos da interface `AppDataContextType`;
2. linha 112 — a declaração `useState<Order | null>(null)`;
3. linha 127 — a chamada de reset dentro do effect de troca de token; manter o resto do effect (incluindo `resetResumo()`, o `queryClient.removeQueries` e `previousTokenRef.current = accessToken`) e o array de deps `[accessToken, resetResumo, queryClient]` inalterados;
4. linhas 215-216 — as duas chaves dentro do objeto do `useMemo` de `contextValue`;
5. linha 229 — a entrada correspondente no array de dependências desse `useMemo`.

Por fim, na linha 19, reduzir `import type { EvolucaoFaturamento, Order } from '@/shared/types/models';` para importar apenas `EvolucaoFaturamento` — `Order` fica sem uso neste arquivo após as remoções acima.

Não alterar `features/pedidos/model/app-data-provider.test.tsx`: ele não referencia nenhum dos identificadores removidos.

Rodar a suíte completa e comparar com o baseline: as ~5 falhas pré-existentes do vitest não contam como regressão desta mudança (confirmar com `git stash` se houver dúvida sobre alguma).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && test "$(grep -rn 'OrderDetailModal\|selectedOrderDetails' app features widgets shared entities lib 2>/dev/null | grep -v 'ProductGradeDetailModal' | wc -l)" -eq 0 && pnpm test</automated>
  </verify>
  <done>Os dois arquivos do modal não existem mais; `pnpm typecheck` e `pnpm lint` passam limpos; a busca no código-fonte não encontra mais o componente morto nem o estado de contexto (o filtro preserva as ocorrências legítimas de `ProductGradeDetailModal`); `pnpm test` não tem nenhuma falha além das já instáveis no baseline.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Nenhuma nova | Remoção de código morto no cliente; não cria, move nem altera nenhuma fronteira de confiança existente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-M3A-01 | Information Disclosure | `OrderDetailModal` (removido) | mitigate | A remoção **reduz** superfície: o componente renderizava dados de pedido por cliente e deixa de existir no bundle |
| T-M3A-02 | Denial of Service | `OrdersList` / páginas de pedidos, histórico e alertas | accept | Alteração puramente subtrativa; o gate real é `pnpm typecheck` + `pnpm test` verdes ao fim de cada task |
| T-M3A-SC | Tampering | npm/pnpm installs | accept | Nenhum pacote instalado, atualizado ou removido neste plano — `package.json` e o lockfile não são tocados |

</threat_model>

<verification>
Todos os comandos rodam a partir de `frontend/` (raiz do app front-end, onde vive o `package.json`), e todos os caminhos citados no plano são relativos a essa raiz.

1. `pnpm typecheck` — sem erros.
2. `pnpm lint` — sem erros novos (em especial, nenhum import ou variável não usada em `app-data-provider.tsx` e `orders-list.tsx`).
3. `pnpm test` — comparar contra o baseline; nenhuma falha nova atribuível a esta mudança.
4. Verificação manual rápida (opcional, se o dev subir `pnpm dev`): abrir `/pedidos`, `/historico` e `/alertas`; a tabela renderiza normalmente e o clique numa linha continua abrindo o `ProductGradeDetailModal`.
</verification>

<success_criteria>
- 2 arquivos apagados, 10 arquivos limpos, zero mudança de comportamento observável.
- Nenhuma referência remanescente ao modal morto, à prop de abertura ou ao par de estado do contexto em código-fonte.
- O fluxo vivo (`ProductGradeDetailModal` via clique de linha) permanece byte-idêntico.
- Commits atômicos em Conventional Commits, um por task, escopo `pedidos` (ex.: `refactor(pedidos): remover prop morta onViewOrder de OrdersList` e `refactor(pedidos): apagar OrderDetailModal e estado de contexto órfão`).
- **Não fazer push sem pedir permissão explícita** (regra do projeto: só `develop`/`main`, sem branch nova, sem PR).
</success_criteria>

<output>
Criar `.planning/quick/260827-m3a-remover-fluxo-morto-de-detalhe-de-pedido/260827-m3a-SUMMARY.md` ao concluir.
</output>
