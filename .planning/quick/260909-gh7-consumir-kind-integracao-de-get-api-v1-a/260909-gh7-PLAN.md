---
phase: 260909-gh7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types/models.ts
  - features/pedidos/ui/priority-alerts-card.tsx
  - features/pedidos/ui/priority-alerts-card.test.tsx
autonomous: true
requirements: [QUICK-260909-gh7]

must_haves:
  truths:
    - "Um alerta vindo de GET /api/v1/alertas com kind 'integracao' e category 'integracao_banco_de_dados' aparece na lista de Alertas com o badge de categoria escrito 'Problemas de integracao' (com acento no arquivo) — nao a string bruta integracao_banco_de_dados (D-02)"
    - "Um alerta com category 'integracao_estoque' recebe exatamente a MESMA etiqueta — as duas fontes ficam agrupadas sob um rotulo comum, e a distincao banco-versus-estoque continua vindo so do title/message que o backend manda pronto (D-02)"
    - "A severidade continua saindo de al.type sem nenhuma linha alterada no bloco de cor/icone: o aviso de banco (type error) sai com moldura destructive e icone X, o de estoque (type warning) com moldura warning e icone AlertTriangle (D-04)"
    - "A pilula nova de filtro mostra apenas alertas com kind === 'integracao' — o predicado e kind, nunca category, para que uma terceira fonte de integracao futura entre no filtro sem tocar neste arquivo de novo (D-03)"
    - "As cinco pilulas de negocio existentes (Todos, Linx, Pedidos/OR, Acesso, Estoque & Credito) continuam filtrando por category exatamente como antes, e nenhuma delas passa a mostrar alerta de integracao — em particular Estoque & Credito mostra anomalia_estoque mas NAO integracao_estoque (D-03)"
    - "A pilula Todos mostra alertas de negocio e de integracao juntos, na ordem em que o servidor mandou — nenhuma secao, cabecalho ou reordenacao foi introduzida (D-02)"
    - "OrderAlert ganhou kind como campo OPCIONAL: nenhum call site existente que constroi ou consome OrderAlert precisou mudar, e pnpm typecheck fica limpo (D-01)"
    - "O refetch em tempo real do aviso de estoque funciona sem uma linha de codigo novo: o topico alerts ja esta ligado em use-alerts-communications.ts e o realtime-provider decide refetch so pelo par (topic, sequence), ignorando event_type — as suites existentes de realtime continuam verdes sem edicao (D-05)"
    - "Nenhum arquivo dentro de design-system/ foi modificado (D-06), e features/pedidos/api/alertas.api.ts, features/pedidos/model/use-alerts-communications.ts e features/realtime/ terminam com blob inalterado (D-05, D-07)"
    - "pnpm test, pnpm typecheck e pnpm lint limpos, sem warning novo"
  artifacts:
    - path: "shared/types/models.ts"
      provides: "Campo kind opcional na interface OrderAlert — unico ponto de tipagem que a mudanca precisa, ja que nao existe transform de campo em nenhum ponto do caminho de fetch"
      contains: "kind?:"
    - path: "features/pedidos/ui/priority-alerts-card.tsx"
      provides: "Etiqueta comum das duas categorias de integracao no mapa CATEGORY_LABELS, pilula de filtro nova em FILTER_OPTIONS, branch de filtro por kind no useMemo de visibleAlerts e bloco correspondente no guia informativo"
      contains: "integracao_banco_de_dados"
    - path: "features/pedidos/ui/priority-alerts-card.test.tsx"
      provides: "Suite co-localizada nova (nao existia teste para este componente) cobrindo etiqueta das duas fontes, severidade por type, comportamento do filtro novo e nao-regressao das pilulas de negocio"
      min_lines: 60
  key_links:
    - from: "features/pedidos/ui/priority-alerts-card.tsx"
      to: "shared/types/models.ts (OrderAlert.kind)"
      via: "predicado do filtro de integracao no useMemo de visibleAlerts"
      pattern: "kind === 'integracao'"
    - from: "features/pedidos/ui/priority-alerts-card.tsx"
      to: "CATEGORY_LABELS"
      via: "lookup existente CATEGORY_LABELS[al.category] || al.category no badge de categoria"
      pattern: "integracao_banco_de_dados"
    - from: "features/pedidos/model/use-alerts-communications.ts (NAO editar)"
      to: "features/realtime/model/realtime-provider.tsx"
      via: "useRealtimeResourceRefresh no topico alerts — caminho ja pronto que o evento alerts.changed.v1 do backend reaproveita sem codigo novo"
      pattern: "topic: 'alerts'"
---

<objective>
Fazer a aba Alertas exibir as duas linhas técnicas novas que `GET /api/v1/alertas` passou a devolver (`kind: "integracao"`), agrupadas sob a etiqueta comum **"Problemas de integração"** e isoláveis por uma pílula de filtro própria.

Purpose: o backend (quick task irmã `260909-ek7`, já mesclada em `develop` e no `origin`) começou a publicar avisos automáticos de banco de dados instável e de estoque do Databricks indisponível no mesmo endpoint de alertas. Hoje o frontend já renderiza essas linhas — mas com a `category` crua aparecendo no badge (`integracao_banco_de_dados`), porque a categoria não está no mapa de etiquetas, e sem nenhuma forma de isolá-las no filtro.

Output: `kind` opcional na interface `OrderAlert`, duas entradas novas em `CATEGORY_LABELS`, uma pílula de filtro nova com predicado por `kind`, um bloco correspondente no guia informativo, e uma suíte vitest co-localizada nova para o componente.

**Não é um redesenho.** São quatro edições cirúrgicas em dois arquivos de produção mais um arquivo de teste novo. Sem componente novo, sem abstração nova, sem seção ou cabeçalho de agrupamento visual, sem endpoint novo, sem mudança no caminho de fetch e sem mudança no realtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@docs/design-system.md

@shared/types/models.ts
@features/pedidos/ui/priority-alerts-card.tsx
@features/pedidos/ui/modals/new-message-modal.test.tsx
</context>

<state_of_the_code>
Levantamento **confirmado no código em 2026-09-09**. Não redescobrir — só reconferir o número de linha exato antes de editar, porque o arquivo pode ter andado.

**`shared/types/models.ts`** — `AlertCategory` é um union fechado de 10 categorias de negócio nas linhas 79-89. `OrderAlert` está nas linhas 91-100, com `category?: AlertCategory | string`. Esse `| string` é o motivo pelo qual `"integracao_banco_de_dados"` já compila hoje sem erro de tipo: **só falta o campo `kind`.**

**`features/pedidos/ui/priority-alerts-card.tsx`** (259 linhas — é o componente-coração da mudança):
- `CATEGORY_LABELS: Record<string, string>` nas linhas 28-39, 10 entradas de negócio.
- `FILTER_OPTIONS` nas linhas 41-47: 5 pílulas — `todos`, `linx`, `pedidos`, `acesso`, `estoque`. **Convenção observada: a pílula usa a forma curta ("Linx", "Acesso", "Estoque & Crédito") e o badge de categoria usa a longa ("Linx ERP", "Acesso Usuários", "Divergência Estoque").**
- `visibleAlerts` — `useMemo` nas linhas 62-75: cadeia de `if` sobre `filterCategory`, todos os branches filtrando por `a.category`, terminando em `return rawAlerts`.
- Badge de categoria nas linhas 155-159: `CATEGORY_LABELS[al.category] || al.category`, com `variant` derivado de `isError`/`isWarning`.
- Bloco de severidade nas linhas 127-151: `al.type === 'error'` produz moldura `ds-border-destructive` mais ícone `X`; `'warning'` produz `ds-border-warning` mais `AlertTriangle`; qualquer outro valor produz `ds-border-info` mais `Info`. **Este bloco não muda.**
- Guia informativo (`Dialog`) nas linhas 199-256: hoje tem exatamente **4 blocos que espelham 1:1 os 4 grupos de pílulas de negócio** (Linx / Pedidos-OR / Acesso & Fila / Estoque & Crédito). Cada bloco repete a mesma estrutura: wrapper `p-3 rounded-control bg-... border ds-border-... space-y-1`, bolinha `h-2 w-2 rounded-full`, título em negrito, parágrafo `text-muted-foreground` terminando em `Ação:` dentro de `<strong>`.
- Não existe componente `AlertRow` separado — o JSX da linha é inline dentro do `.map` sobre `visibleAlerts`.
- Cada linha já emite `id={`alert-badge-item-${idx}`}` — é o gancho estável para asserções de severidade no teste.
- `useLoadMore` (`shared/hooks/useLoadMore.ts`) tem `LOAD_MORE_STEP = 25`, então em teste sem a prop `onLoadMore` e com poucos fixtures a lista renderiza inteira, sem janela cortando nada.

**Nada a mudar aqui, verificado:**
- `features/pedidos/api/alertas.api.ts` é passa-tudo: `parseCursorPage<OrderAlert>(data, 'alertas')` normaliza só o envelope paginado, nunca os campos internos da linha. Sem transform, sem mapper, sem allow-list de campos — `kind` chega ao componente sozinho, basta o tipo.
- Realtime: `useRealtimeResourceRefresh({ topic: 'alerts', revision: topicRevisions.alerts, ... })` já está montado nas linhas 55-57 de `features/pedidos/model/use-alerts-communications.ts`, e `features/realtime/model/realtime-provider.tsx` decide refetch só pelo par `(topic, sequence)` — o `event_type` do evento é ignorado nessa decisão. Portanto `alerts.changed.v1` já dispara o refetch REST. Cobertura já existente: 13 casos em `features/pedidos/model/use-realtime-resource-refresh.test.tsx`, e o tópico `alerts` exercitado com bump de revisão em `use-alerts-communications.test.tsx`. **Nenhum teste novo de realtime é necessário — a exigência é manter essas suítes verdes sem editá-las.**
- Design system: o `Badge` (variantes `destructive` / `warning` / `outline`) já entrega a cor certa a partir do `type` que o backend manda. Nenhum token ou componente novo. `docs/design-system.md` (linhas 19-25) proíbe editar `design-system/` — é vendor versionado, e a edição se perderia no próximo sync.

**Não existe** `features/pedidos/ui/priority-alerts-card.test.tsx` hoje. O arquivo de teste da Task 1 é novo.

**Fora de escopo, deliberado — não "corrigir":** o badge do cabeçalho ("N Críticos", linhas 94-98) usa `total ?? alerts.length` e portanto passa a contar também os avisos de integração. Nenhuma decisão pediu mudança ali; deixar como está e não tocar.

**Nota de ferramenta:** o `.claude/CLAUDE.md` exige consultar `backstage_get_coding_standards` (servidor MCP `backstage`) antes de editar arquivos deste repo. O `.mcp.json` existe, mas a ferramenta **não estava disponível na sessão de planejamento** — o plano seguiu as convenções observadas no próprio código. O executor deve tentar chamá-la; se responder, conferir o plano contra ela antes de editar; se não estiver configurada ou autenticada, prosseguir e registrar o fato no SUMMARY.
</state_of_the_code>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Campo kind no OrderAlert e suite RED do PriorityAlertsCard</name>
  <files>shared/types/models.ts, features/pedidos/ui/priority-alerts-card.test.tsx</files>

  <behavior>
    Casos que o arquivo de teste novo deve exercer. Todos escritos ANTES da implementação da Task 2; os que dependem dela precisam FALHAR nesta task.

    - Etiqueta de banco: alerta com `category: 'integracao_banco_de_dados'` renderiza um badge com o texto exato "Problemas de integração", e a string crua `integracao_banco_de_dados` não aparece em nenhum lugar do documento. (FALHA na Task 1.)
    - Etiqueta de estoque: alerta com `category: 'integracao_estoque'` renderiza o MESMO texto — assertar 2 ocorrências de "Problemas de integração" quando os dois alertas de integração estão na lista. (FALHA na Task 1.)
    - Severidade preservada: com os dois alertas de integração renderizados, a linha do de banco (`type: 'error'`) carrega a classe `ds-border-destructive` e a do de estoque (`type: 'warning'`) carrega `ds-border-warning`. (PASSA já na Task 1 — é o guarda de regressão da D-04, que afirma que a severidade já sai certa sem mudança nenhuma.)
    - Filtro por kind: clicar na pílula "Integração" deixa na tela só os dois alertas de integração e remove os de negócio. Assertar pelo `title` de cada alerta, não por contagem de nós. (FALHA na Task 1.)
    - Não-regressão das pílulas de negócio: clicar em "Estoque & Crédito" mostra o alerta de `anomalia_estoque` e NÃO mostra o de `integracao_estoque`. É justo o par que quebraria se alguém filtrasse por substring de `category` em vez de por `kind`. (PASSA já na Task 1.)
    - Pílula Todos: sem clicar em nada, os alertas de negócio e os de integração aparecem todos. (PASSA já na Task 1.)
    - Guia informativo: clicar no gatilho de ajuda (o `Pressable` com `title="Guia Informativo de Alertas"`) abre o diálogo e o texto "Problemas de integração" aparece dentro dele. (FALHA na Task 1.)
  </behavior>

  <action>
    Duas edições, nesta ordem.

    **1. `shared/types/models.ts`** — acrescentar UM campo à interface `OrderAlert` (por volta das linhas 91-100), depois de `affectedCount`: `kind` opcional, com o union literal inline de `'negocio'` e `'integracao'`, conforme D-01. Opcional (`?`) de propósito: o backend manda `"negocio"` como default em todas as 8 categorias de negócio, mas o campo opcional garante que nenhum call site que hoje monta um `OrderAlert` à mão (fixtures de teste, projeções derivadas de `orders` no provider) precise mudar.

    Escrever um comentário de UMA linha acima do campo dizendo o que ele distingue: linha de negócio (projeção de pedido) versus aviso técnico automático de integração (banco de dados instável ou estoque do Databricks indisponível), cujos `title`/`message` já chegam do backend em linguagem simples, prontos para exibir sem tradução.

    NÃO criar type alias exportado (`AlertKind`) — o union inline basta e nada mais no repo precisa referenciá-lo. NÃO acrescentar `integracao_banco_de_dados` nem `integracao_estoque` ao union `AlertCategory`: aquele union descreve as categorias de negócio, e `category` já aceita `| string`, então as duas novas passam sem erro. Deixar `AlertCategory` byte a byte como está.

    **2. `features/pedidos/ui/priority-alerts-card.test.tsx`** (arquivo novo) — suíte co-localizada seguindo o padrão de `features/pedidos/ui/modals/new-message-modal.test.tsx`: `import React from 'react'`, imports nomeados de `vitest` (`describe`, `expect`, `it`), `render` / `screen` / `fireEvent` de `@testing-library/react`, `userEvent` de `@testing-library/user-event`, e uma factory local `makeAlert` no molde da `makeOrder` daquele arquivo (assinatura `Partial<OrderAlert> & Pick<OrderAlert, 'id'>` devolvendo `OrderAlert` com defaults sensatos).

    Nenhum `vi.mock` é necessário: `PriorityAlertsCard` só recebe props, e as dependências (`useLoadMore`, `Badge`, `Card`, `Pressable`, `EmptyState`, `Dialog`, `FeedCardHeader`) rodam de verdade — os polyfills de `ResizeObserver` e `IntersectionObserver` já estão em `vitest.setup.ts`. Renderizar sempre SEM a prop `onLoadMore`, para o componente cair no caminho `useLoadMore`, onde `LOAD_MORE_STEP = 25` renderiza os fixtures inteiros.

    Fixtures: um array de 4 alertas — dois de negócio (um de `category: 'linx'` com `type: 'error'` e um de `category: 'anomalia_estoque'` com `type: 'warning'`, ambos `kind: 'negocio'`) e os dois de integração reais do backend: `category: 'integracao_banco_de_dados'` com `type: 'error'`, `kind: 'integracao'`, `title: 'Instabilidade no banco de dados'`, `time: 'Agora'`, `affectedCount: 3`; e `category: 'integracao_estoque'` com `type: 'warning'`, `kind: 'integracao'`, `title: 'Estoque indisponível'`, `time: 'Agora'`, `affectedCount: 1`. Dar a cada alerta um `title` distinto — as asserções de filtro consultam por `title`, que é estável, em vez de contar nós do DOM.

    Para as asserções de severidade, consultar a linha pelo `id` que o componente já emite (`alert-badge-item-0`, `alert-badge-item-1`, ...) via `container.querySelector` e assertar com `toHaveClass`. Comentar no arquivo POR QUE a asserção é sobre classe CSS: a severidade não tem outra saída observável no DOM (a moldura e a variante do badge são a única manifestação), e este caso existe como guarda de regressão da decisão de que a cor já vem certa do `type`.

    Para o caso do guia informativo, abrir o diálogo com `fireEvent.click` no gatilho, não com `userEvent`: o gatilho é um `Pressable` comum acionando `setShowInfoModal`, mas o `DialogContent` do Radix monta em portal e o jsdom não implementa pointer capture. Consultar o conteúdo com `screen`, que alcança portais. **Escape hatch delimitado:** se o `Dialog` do Radix não montar no jsdom nem com `fireEvent`, remover SOMENTE esta asserção do guia, deixar um comentário no arquivo explicando o motivo, e registrar no SUMMARY — não gastar tempo lutando com o Radix e não mockar o `Dialog`.
  </action>

  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test -- priority-alerts-card.test.tsx</automated>
  </verify>

  <done>
    `pnpm typecheck` sai 0 — o campo opcional não quebrou nenhum call site existente. `pnpm test -- priority-alerts-card.test.tsx` sai NÃO-zero, com falhas exatamente nos 4 casos que dependem da Task 2 (etiqueta de banco, etiqueta de estoque, filtro por kind, guia informativo) e com os 3 casos de guarda de regressão (severidade, não-regressão de Estoque & Crédito, pílula Todos) já passando. Nenhum outro arquivo de teste do repo mudou de resultado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Etiqueta comum, pilula de filtro por kind e verde geral</name>
  <files>features/pedidos/ui/priority-alerts-card.tsx</files>

  <action>
    Quatro edições em UM arquivo, todas encaixando em estrutura que já existe nele. Sem componente novo, sem helper novo, sem reestruturação.

    **1. `CATEGORY_LABELS`** (por volta das linhas 28-39) — acrescentar duas entradas, ambas com o MESMO valor "Problemas de integração": chave `integracao_banco_de_dados` e chave `integracao_estoque` (D-02). Separar do bloco de negócio com um comentário curto explicando que as duas fontes compartilham a etiqueta de propósito, porque a distinção banco-versus-estoque já fica clara no `title`/`message` que o backend manda pronto — o badge de categoria só precisa agrupar. Estender este mapa é o caminho mais idiomático: o badge (linha 157) já faz `CATEGORY_LABELS[al.category] || al.category`, então essas duas linhas de mapa resolvem a exibição sem tocar no JSX da linha.

    **2. `FILTER_OPTIONS`** (por volta das linhas 41-47) — acrescentar uma sexta pílula ao final: chave `integracao`, label "Integração" (D-03). Label curto de propósito: as pílulas existentes usam a forma curta ("Linx", "Acesso") enquanto o badge de categoria usa a longa ("Linx ERP", "Divergência Estoque"); a pílula é a última de uma fileira com scroll horizontal (`overflow-x-auto no-scrollbar whitespace-nowrap`), e "Problemas de integração" inteiro empurraria as outras cinco para fora da viewport. A forma longa continua onde importa — no badge de cada linha.

    **3. `visibleAlerts`** (o `useMemo` por volta das linhas 62-75) — acrescentar UM branch, depois do branch de `estoque` e antes do `return rawAlerts` final: quando `filterCategory === 'integracao'`, devolver `rawAlerts.filter((a) => a.kind === 'integracao')`. **Filtrar por `kind`, nunca por `category`** (D-03): assim uma terceira fonte de integração que o backend adicione no futuro entra na pílula sozinha, sem ninguém precisar voltar neste arquivo. Comentar essa intenção em uma linha. Não mexer em nenhum dos branches de negócio existentes — eles continuam filtrando por `category` e, por consequência, continuam nunca mostrando alerta de integração.

    **4. Guia informativo** (o `Dialog` por volta das linhas 199-256) — acrescentar um quinto bloco ao final da `<div className="space-y-2.5">`, copiando exatamente a estrutura dos 4 existentes (wrapper `p-3 rounded-control ... space-y-1`, bolinha `h-2 w-2 rounded-full`, título em negrito, parágrafo `text-muted-foreground` com `Ação:` em `<strong>`). Usar o estilo NEUTRO do quarto bloco (`bg-muted/15 border ds-border-divider`, bolinha `bg-foreground`, título `text-foreground`), porque integração não tem severidade fixa: o aviso de banco é `error` e o de estoque é `warning`, então nenhuma das cores de severidade seria honesta como rótulo do grupo.

    Título do bloco: "Problemas de integração". Parágrafo: "Avisos técnicos automáticos do sistema — banco de dados instável ou estoque indisponível." seguido de `Ação:` em `<strong>` e "nenhuma ação no automation-OR; aguardar a normalização e acionar o suporte técnico se persistir."

    Este quinto bloco não é escopo extra: o guia espelha 1:1 os grupos de pílulas hoje (4 blocos para 4 grupos de negócio), então deixar a pílula nova sem entrada correspondente seria o próprio guia ficando desatualizado no mesmo commit que cria a pílula.

    **Proibições desta task.** Não usar `dangerouslySetInnerHTML` em `title` nem em `message` — são strings do servidor e o escape default do React é a mitigação (ver `<threat_model>`). Não tocar no bloco de severidade das linhas 127-151 (D-04). Não criar seção, cabeçalho ou agrupamento visual na lista — ela continua plana (D-02). Não tocar em `features/pedidos/api/alertas.api.ts`, em `features/pedidos/model/use-alerts-communications.ts` nem em nada de `features/realtime/` (D-05, D-07). Não tocar em nada dentro de `design-system/` (D-06). Não editar as suítes de realtime existentes para fazê-las passar — elas já passam, e editá-las seria mascarar regressão.
  </action>

  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm lint</automated>
    <automated>git status --porcelain -- design-system features/realtime features/pedidos/api/alertas.api.ts features/pedidos/model/use-alerts-communications.ts</automated>
    <automated>grep -v '^\s*//' features/pedidos/ui/priority-alerts-card.tsx | grep -c 'Problemas de integra'</automated>
  </verify>

  <done>
    Os 7 casos de `priority-alerts-card.test.tsx` passam, e `pnpm test` fecha verde no repo inteiro — inclusive `use-realtime-resource-refresh.test.tsx` e `use-alerts-communications.test.tsx`, sem que nenhum dos dois tenha sido editado. `pnpm typecheck` e `pnpm lint` saem 0, sem warning novo. O `git status --porcelain` dos caminhos protegidos volta VAZIO (nenhuma linha), provando que `design-system/`, `features/realtime/`, `alertas.api.ts` e `use-alerts-communications.ts` estão intactos. O `grep -c` (com as linhas de comentário filtradas, para que a prosa de comentário não valide a si mesma) conta pelo menos 3 ocorrências de código real da etiqueta: as duas entradas do `CATEGORY_LABELS` e o título do bloco do guia.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → cliente React | `title`, `message` e `category` das linhas novas de `GET /api/v1/alertas` são strings escolhidas pelo servidor e renderizadas na aba Alertas. Superfície nova nesta mudança: nenhuma — as mesmas linhas já são renderizadas hoje; o que muda é só a etiqueta e o filtro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260909-gh7-01 | Tampering / XSS | `priority-alerts-card.tsx`, render de `title` e `message` | mitigate | Manter os dois em interpolação JSX comum (`{al.title}`, `{al.message}`), que o React escapa por default. `dangerouslySetInnerHTML` proibido explicitamente na Task 2. |
| T-260909-gh7-02 | Tampering | lookup `CATEGORY_LABELS[al.category]` sobre um `Record<string, string>` | accept | `category` chega de um enum fechado do nosso próprio backend autenticado; uma chave de prototype (`constructor`, `__proto__`) devolveria um não-string. Exposição idêntica à de hoje — o lookup já existe e esta mudança só acrescenta duas chaves ao mapa, sem alterar o formato do acesso. Endurecer o lookup fica fora do escopo de um quick task. |
| T-260909-gh7-03 | Information disclosure | texto dos avisos técnicos exibido ao usuário de negócio | accept | O backend já entrega `title`/`message` em linguagem simples e sem detalhe de infraestrutura (sem host, stack trace ou string de conexão). O frontend só exibe, não compõe nem enriquece a mensagem. |
| T-260909-gh7-SC | Tampering | supply chain (npm) | mitigate | Zero dependência nova: nenhum `pnpm add`, nenhuma mudança em `package.json` ou lockfile. Sem instalação, o gate de legitimidade de pacote não se aplica; se qualquer task acabar precisando de um pacote novo, parar e escalar antes de instalar. |
</threat_model>

<verification>
Ordem de verificação, do mais barato ao mais caro:

1. `pnpm typecheck` — sai 0. Prova que `kind?` é aditivo e não quebrou call site nenhum.
2. `pnpm test -- priority-alerts-card.test.tsx` — os 7 casos verdes.
3. `pnpm test` — repo inteiro verde. Confirma que as suítes de realtime (`use-realtime-resource-refresh.test.tsx`, `use-alerts-communications.test.tsx`) continuam passando SEM edição, que é a prova concreta da D-05: o evento `alerts.changed.v1` reaproveita um caminho de refetch já testado.
4. `pnpm lint` — sem warning novo.
5. `git status --porcelain -- design-system features/realtime features/pedidos/api/alertas.api.ts features/pedidos/model/use-alerts-communications.ts` — saída vazia (D-05, D-06, D-07).
6. `git diff --stat` — exatamente 3 arquivos: `shared/types/models.ts`, `features/pedidos/ui/priority-alerts-card.tsx`, `features/pedidos/ui/priority-alerts-card.test.tsx`.

<human-check>
Verificação visual opcional, para quando a mantenedora quiser conferir na tela (não bloqueia o commit): reproduzir um aviso de integração de verdade exige o Databricks ou o banco realmente instáveis, então o caminho prático é confiar nos testes de componente. Se ainda assim quiser ver: subir `pnpm dev`, abrir `/alertas` com o backend rodando e conferir que (a) as pílulas continuam em uma linha só, sem quebrar nem cortar, com "Integração" no fim; (b) o badge de categoria de um aviso de integração diz "Problemas de integração"; (c) o guia informativo (ícone de interrogação no cabeçalho do card) mostra os 5 blocos.
</human-check>
</verification>

<success_criteria>
- `kind?: 'negocio' | 'integracao'` na interface `OrderAlert`, e `AlertCategory` inalterado (D-01).
- `integracao_banco_de_dados` e `integracao_estoque` mapeados para a MESMA etiqueta "Problemas de integração" em `CATEGORY_LABELS` (D-02).
- Pílula "Integração" em `FILTER_OPTIONS`, filtrando por `kind === 'integracao'` e não por `category` (D-03).
- Bloco de severidade das linhas 127-151 byte a byte inalterado (D-04).
- Zero linha de código de realtime alterada, e as suítes de realtime existentes verdes sem edição (D-05).
- Zero arquivo modificado dentro de `design-system/` (D-06).
- Nenhum endpoint novo; `features/pedidos/api/alertas.api.ts` inalterado (D-07).
- `pnpm test`, `pnpm typecheck` e `pnpm lint` limpos.
- Diff final restrito a 3 arquivos.
- Commits em Conventional Commits, em branch `feat/` a partir de `develop`, push para `develop` — nunca `main`. **Sem `Co-Authored-By` e sem qualquer menção a Claude na mensagem** (regra permanente deste projeto).
</success_criteria>

<output>
Criar `.planning/quick/260909-gh7-consumir-kind-integracao-de-get-api-v1-a/260909-gh7-SUMMARY.md` ao terminar.

Registrar no SUMMARY: se o `backstage_get_coding_standards` respondeu nesta sessão (e o que ele disse de relevante) ou se não estava disponível; e se a asserção do guia informativo no teste sobreviveu ao jsdom ou foi removida pelo escape hatch da Task 1.
</output>
