---
phase: 260831-lb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/realtime/model/storage.ts
  - features/realtime/model/realtime-provider.tsx
autonomous: true
requirements: [QUICK-260831-lb9]

must_haves:
  truths:
    - "A camada de persistência do cursor realtime (chave, serialização versionada, parser e as três operações de I/O) vive em features/realtime/model/storage.ts"
    - "realtime-provider.tsx não contém nenhuma chamada direta a window.localStorage — todo acesso passa pelas funções do novo módulo"
    - "Os cinco pontos de acesso a storage do provider continuam na mesma ordem, com os mesmos gatilhos e os mesmos comentários explicando por que a falha é tolerada"
    - "O guard de ambiente que hoje protege apenas a remoção feita no logout continua no mesmo lugar e com a mesma condição"
    - "A superfície pública de features/realtime/index.ts continua exatamente RealtimeProvider, useRealtime e o tipo RealtimeTopic"
    - "realtime-provider.test.tsx passa sem nenhuma alteração no arquivo de teste — comportamento idêntico"
    - "A chave gravada no navegador continua sendo exatamente a mesma string de hoje (prefixo + usuário codificado), e a versão aceita na leitura continua 1"
  artifacts:
    - path: "features/realtime/model/storage.ts"
      provides: "Camada de persistência do cursor realtime em localStorage — montagem da chave por usuário, tipo do payload versionado, parser tolerante e as três operações de I/O que engolem falha de storage por contrato"
      exports:
        - "PersistedRealtimeState"
        - "MAX_SEEN_EVENT_IDS"
        - "parsePersistedState"
        - "loadPersistedState"
        - "savePersistedState"
        - "clearPersistedStateStorage"
      min_lines: 90
  key_links:
    - from: "features/realtime/model/realtime-provider.tsx"
      to: "features/realtime/model/storage.ts"
      via: "import das funções de persistência extraídas"
      pattern: "from \"./storage\""
    - from: "features/realtime/model/storage.ts"
      to: "features/realtime/model/protocol.ts"
      via: "predicado de record, coerção de sequência, parser de mapa de tópicos e limite de tamanho de id de evento"
      pattern: "from \"./protocol\""
    - from: "features/realtime/model/storage.ts"
      to: "features/realtime/model/types.ts"
      via: "tipo do mapa por tópico usado no payload persistido"
      pattern: "from \"./types\""
---

<objective>
Segunda fatia de `features/realtime/model/realtime-provider.tsx` (hoje 1070 linhas,
depois da extração de `protocol.ts` na task 260831-kpx): extrair a camada de
persistência do cursor realtime para um módulo novo,
`features/realtime/model/storage.ts`.

É reorganização PURA de código. Diferente da fatia anterior — que só movia funções
já isoladas —, esta também precisa **encapsular I/O que hoje está espalhado**: as
chamadas a `window.localStorage` aparecem em cinco pontos diferentes do provider,
cada uma com seu próprio `try/catch` silencioso. Elas viram três funções wrapper no
módulo novo, e o provider passa a chamá-las. Comportamento observável idêntico:
mesma chave, mesma ordem de operações, mesmas falhas engolidas, mesmos comentários
explicando por quê.

Purpose: hoje, para responder "o que exatamente é persistido e quando", é preciso
caçar cinco trechos em 1070 linhas e reconstruir o formato de cabeça. Concentrando
chave + formato + leitura/escrita/remoção num arquivo de ~110 linhas, a resposta
cabe numa tela — e o provider deixa de repetir o mesmo `try/catch` de storage em
lugares distantes.
Output: `features/realtime/model/storage.ts` novo; `realtime-provider.tsx` reduzido
para ~985 linhas, sem nenhum acesso direto a storage do navegador.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@features/realtime/model/realtime-provider.tsx
@features/realtime/model/protocol.ts
@features/realtime/model/types.ts
@features/realtime/index.ts
</context>

<discovery_notes>
Levantamento feito durante o planejamento contra o código atual (HEAD `2c8dd1d`,
branch `develop`, árvore limpa). O executor pode confiar nestes fatos sem
re-verificar.

1. **Os cinco pontos de acesso a storage, na ordem em que aparecem hoje.** Este é o
   inventário completo — não existe um sexto:

   | # | Onde | Operação | Guard de ambiente | `try/catch` |
   |---|---|---|---|---|
   | A | callback `clearPersistedState` (~l. 325-339) | remoção da chave do usuário ativo | **sim**, condição combinada com a existência do usuário | sim, com comentário |
   | B | efeito de persistência (~l. 348-362) | escrita do payload serializado | não | sim, com comentário |
   | C | efeito de conexão, ramo não-autenticado (~l. 368-378) | remoção | não | sim, com comentário |
   | D | efeito de conexão, ramo de troca de conta (~l. 384-391) | remoção | não | sim, com comentário |
   | E | efeito de conexão, ramo de carga inicial (~l. 395-403) | leitura + parse | não | sim, **sem** comentário |

   Os comentários de A, B, C e D são **quatro textos diferentes**, cada um
   justificando a tolerância à falha naquele contexto específico. Nenhum é
   descartável: preservá-los é requisito explícito desta tarefa.

2. **O guard de ambiente existe em A e só em A, e deve continuar assim.** Nos casos
   C, D e E o código roda dentro de um efeito (portanto sempre com `window`
   disponível) e a falha hipotética cairia no `catch` de qualquer jeito. Adicionar
   o guard dentro dos wrappers seria neutro no resultado, mas seria mudança de
   código não pedida — **não faça**. Em A, o guard fica onde está, no `if` do
   provider, porque ele protege apenas a remoção e não as demais instruções do
   callback (abortar controllers, baixar a flag de storage pronto, resetar estado).

3. **O que se move e o que fica.** Vão para `storage.ts`: as duas constantes de
   storage (prefixo da chave e versão do payload), o tipo do payload persistido, a
   função que monta a chave e o parser tolerante. Ficam no provider, intocadas,
   todas as outras constantes do topo do arquivo — teto de tamanho de mensagem,
   intervalo de poll de status, timeouts de abertura de socket e de handshake de
   replay, margem e TTL de ticket, e a janela de estabilidade da reconexão.

4. **A constante de teto de ids de evento memorizados é compartilhada e por isso
   precisa ser exportada**, não apenas movida: o parser a usa para truncar a lista
   lida (`.slice(-N)`), mas o provider também a usa em dois pontos que **ficam** —
   a poda da fila de ids vistos no `onmessage` (~l. 236) e a montagem do payload no
   efeito de persistência (~l. 355). Mesmo padrão já adotado na task anterior para
   `MAX_EVENT_ID_LENGTH`/`DEFAULT_HEARTBEAT_SECONDS` em `protocol.ts`.

5. **Dois imports do provider ficam órfãos e precisam sair.** Depois que o parser
   sai do arquivo, `MAX_EVENT_ID_LENGTH` (usado hoje só na linha 105) e
   `parseTopicMap` (usado hoje só nas linhas 96-97) não têm mais nenhum consumidor
   em `realtime-provider.tsx`. Ambos continuam exportados por `protocol.ts` —
   quem passa a consumi-los é `storage.ts`. Já `isRecord` e `toSequence`
   **permanecem** no import de `./protocol` do provider: além do parser, são usados
   no `onmessage` do socket (l. 764, 774, 777, 802).
   Atenção: o ESLint deste projeto trata variável não usada como **warning**, não
   erro — `pnpm lint` passaria mesmo deixando os órfãos. Por isso a Task 2 tem gate
   de grep explícito para isso.

6. **`storage.ts` importa de `protocol.ts`, nunca o contrário.** O parser precisa de
   quatro símbolos que já são exportados de lá — o predicado de record, a coerção de
   sequência, o parser de mapa de tópicos e o limite de tamanho de id de evento. A
   direção `storage → protocol → types` é acíclica; não crie import de volta em
   `protocol.ts`. **`protocol.ts` não deve ser tocado nesta tarefa** (blob esperado
   `aa48dcf`).

7. **`realtime-provider.test.tsx` NÃO importa nada de privado** — só
   `RealtimeProvider`/`useRealtime` de `./realtime-provider`, o tipo
   `RealtimeContextValue` de `./types` e o namespace `* as realtimeApi` de
   `../api/realtime-api`. Ele **não deve ser tocado em nenhuma linha**: é o
   instrumento de medição desta tarefa, não o objeto dela.

8. **Os testes travam a chave literal do storage em seis lugares.** Eles substituem
   `window.localStorage` por um `MemoryStorage` em memória (via `defineProperty` no
   `beforeEach`) e afirmam contra a string `system-automation:realtime:v1:user-1`
   diretamente (l. 421, 766, 1058, 1137, 1415, 1432). Consequências práticas:
   - o prefixo e o esquema de montagem da chave têm que sair **byte a byte iguais**;
   - o acesso tem que continuar sendo feito **através do objeto `window`**, não pelo
     binding global nu, para casar com a forma como o mock é instalado.

9. **A leitura tem dois níveis de `try/catch` e os dois precisam sobreviver.** O
   parser já tem um `catch` interno que cobre a desserialização malformada; o ponto
   E tem um `catch` externo que cobre a própria leitura do storage lançar. São
   camadas diferentes com causas diferentes — não colapse uma na outra. Da mesma
   forma, na escrita (ponto B) a serialização acontece **dentro** do `try`: um
   payload não serializável hoje é engolido junto com a falha de quota, e precisa
   continuar sendo.

10. **`storage.ts` não leva a diretiva de client.** Mesmo tocando API de navegador,
    o toque acontece só dentro de corpo de função chamada a partir de efeito —
    nunca na avaliação do módulo. `protocol.ts` e `realtime-api.ts` já são
    precedentes de módulos sem diretiva importados pelo provider. Existe um teste de
    render no servidor na suíte; como efeitos não rodam ali, nenhum destes caminhos
    é exercitado durante SSR.

11. **Não renomeie o callback `clearPersistedState` do provider.** Ele é parte do
    contrato público de `RealtimeContextValue` (documentado em `types.ts` e usado no
    fluxo de logout) e faz bem mais do que mexer em storage. O wrapper novo tem nome
    deliberadamente diferente para não haver confusão entre os dois.

**Armadilha de ferramenta 1 (herdada da task 260828-fz2):** `pnpm test -- <arquivo>`
NÃO filtra, roda a suíte inteira. Use `npx vitest run <filtro>` direto, e lembre que
o filtro é REGEX contra o caminho. `npx vitest run realtime` casa com os dois
arquivos de teste do feature (`realtime-provider.test.tsx` e `realtime-api.test.ts`).

**Armadilha de ferramenta 2 (herdada da task 260831-kpx):** `frontend` é
um **repositório git próprio**, aninhado na pasta de trabalho que também contém o
backend. Portanto `cd "$(git rev-parse --show-toplevel)"` puro já aterrissa na raiz
do frontend a partir de qualquer subdiretório dele — não concatene o nome da pasta
depois, isso resolve para caminho inexistente e derruba a cadeia `&&` inteira antes
de qualquer gate rodar.

**Armadilha de ferramenta 3 (observada na execução da task 260831-kpx):** a suíte
completa tem instabilidade pré-existente sob carga em
`widgets/pedido-dashboard/ui/orders-list.pagination.test.tsx` (timeouts de
`waitFor`). Se aparecer falha lá na Task 3, re-execute **só aquele arquivo** e
confirme que passa isolado antes de suspeitar desta mudança — nenhum arquivo fora de
`features/realtime/model/` é tocado por este plano.

**Git:** commits vão para `develop` (nunca `main`), um por task, e **nenhum push
acontece sem aprovação explícita da desenvolvedora**.
</discovery_notes>

<tasks>

<task type="auto">
  <name>Task 1: Criar features/realtime/model/storage.ts com o bloco puro copiado verbatim e os três wrappers de I/O</name>
  <files>features/realtime/model/storage.ts (novo)</files>
  <action>
Crie `features/realtime/model/storage.ts`. Sem diretiva de client, sem import de
react/next/next-auth (ver `<discovery_notes>` item 10).

Abra com um comentário de cabeçalho curto em português, no tom dos comentários já
existentes no feature, dizendo que o módulo concentra a persistência do cursor
realtime no navegador — montagem da chave por usuário, formato versionado do
payload, e as leituras/escritas que toleram storage indisponível — e que ele existe
para que o provider não repita o mesmo tratamento de falha em cinco pontos
distantes do arquivo.

Imports do módulo, e nenhum outro:
- de `./types`: o tipo do mapa por tópico (import de tipo);
- de `./protocol`: `MAX_EVENT_ID_LENGTH`, `isRecord`, `parseTopicMap`, `toSequence`
  (imports de valor, exceto o limite que é constante).

**Bloco 1 — constantes.** Copie do topo de `realtime-provider.tsx`, com exatamente
os mesmos valores que têm hoje:
- a constante de prefixo da chave — **privada**, sem export;
- a constante de versão do payload — **privada**, sem export;
- a constante de teto de ids de evento memorizados (valor 512) — **exportada**,
  porque o provider continua precisando dela em dois pontos que ficam lá (ver
  `<discovery_notes>` item 4).

Nenhuma outra constante do provider vem junto (item 3).

**Bloco 2 — tipo do payload.** Traga o tipo hoje local do provider que descreve o
estado persistido (versão literal 1, sequência, revisões por tópico, revisões lidas,
lista de ids de evento vistos), com o **mesmo nome** e agora **exportado**: o
provider volta a importá-lo para anotar o objeto que monta antes de gravar.

**Bloco 3 — funções puras, copiadas verbatim.** Traga as duas declarações abaixo do
provider sem refatorar nada — mesmos corpos, mesmas comparações, mesmos limites:
- a função que monta a chave a partir da identificação do usuário (concatena o
  prefixo com o valor codificado por `encodeURIComponent`) — mantenha-a **privada**,
  sem export: depois desta tarefa ninguém fora do módulo monta chave, e exportá-la
  reabriria exatamente o acoplamento que estamos fechando. O formato produzido é
  travado pelos testes (item 8), então não mexa em um caractere;
- o parser tolerante que recebe o conteúdo cru (`string | null`) e devolve o payload
  ou nulo — **exportado**, por ser a metade determinística do módulo e o ponto
  natural de teste isolado futuro (mesmo critério que levou os parsers de protocolo
  a serem exportados na task anterior). Preserve integralmente: a saída antecipada
  para conteúdo vazio, a checagem de versão, as três coerções, a filtragem dos ids
  por tipo/comprimento, o truncamento pelo teto e o `catch` que devolve nulo.

**Bloco 4 — os três wrappers de I/O (esta é a parte nova).** Cada um encapsula um
dos acessos que hoje estão soltos no provider. Todos recebem a identificação do
usuário e resolvem a chave internamente. Todos engolem falha de storage **por
contrato** — documente isso em uma ou duas linhas de comentário no módulo, deixando
claro que quem chama não tem o que fazer com o erro: o realtime segue funcional sem
persistência.

- `loadPersistedState(userKey: string): PersistedRealtimeState | null` — lê o
  conteúdo cru pela chave do usuário e devolve o resultado do parser; se a **leitura
  em si** lançar, devolve nulo. Os dois níveis de tratamento precisam coexistir: o
  do parser cobre conteúdo malformado, este cobre storage bloqueado (item 9).
- `savePersistedState(userKey: string, state: PersistedRealtimeState): void` —
  serializa e grava na chave do usuário. A **serialização acontece dentro** do
  bloco protegido, como hoje (item 9). Não devolve nada e não sinaliza falha.
- `clearPersistedStateStorage(userKey: string): void` — remove a chave do usuário.
  Nome deliberadamente distinto do callback homônimo mais curto que vive no provider
  e que faz muito mais coisa (item 11) — não use o nome curto aqui.

Regras que valem para os três: acesse o storage **através do objeto `window`**, na
mesma forma usada hoje, porque é assim que o mock dos testes é instalado (item 8);
não adicione guard de ambiente dentro deles (item 2); não adicione log, telemetria,
valor de retorno de sucesso/erro, `async`, nem argumento opcional novo.

NÃO toque em `realtime-provider.tsx` nesta task — ele continua com as cópias locais
e segue compilando e passando os testes normalmente. A duplicação temporária é
esperada e some na Task 2.

NÃO toque em `protocol.ts` (item 6), `types.ts`, `realtime-api.ts`,
`features/realtime/index.ts` (a superfície pública não muda) nem no arquivo de teste
(item 7).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f package.json && test -f features/realtime/model/storage.ts && npx tsc --noEmit && npx vitest run realtime && test "$(grep -cE '^import .*(react|next/|next-auth)' features/realtime/model/storage.ts)" = "0" && test "$(grep -cE '^(export )?(function|type|const) (storageKey|parsePersistedState|loadPersistedState|savePersistedState|clearPersistedStateStorage|PersistedRealtimeState|MAX_SEEN_EVENT_IDS)' features/realtime/model/storage.ts)" = "7" && grep -q 'window\.localStorage\.getItem' features/realtime/model/storage.ts && grep -q 'window\.localStorage\.setItem' features/realtime/model/storage.ts && grep -q 'window\.localStorage\.removeItem' features/realtime/model/storage.ts && grep -q 'system-automation:realtime:v1' features/realtime/model/storage.ts && grep -q 'encodeURIComponent' features/realtime/model/storage.ts && test "$(git hash-object features/realtime/model/protocol.ts)" = "aa48dcf0643f63ae498342c00e121e270b6c7f4b" && test "$(git hash-object features/realtime/model/realtime-provider.test.tsx)" = "00adf530d57aa2728919338fa46ef8e1009561c2"</automated>
  </verify>
  <done>
`features/realtime/model/storage.ts` existe com as duas funções puras (montagem de
chave privada, parser exportado), os três wrappers de I/O exportados, o tipo do
payload exportado, o teto de ids exportado e as duas constantes de chave/versão
privadas. O módulo não importa react/next/next-auth. `npx tsc --noEmit` limpo e a
suíte de realtime passando (o provider ainda tem as cópias locais neste ponto — é
esperado). `protocol.ts` e o arquivo de teste com blob inalterado.
  </done>
</task>

<!-- planner-discipline-allow: react -->
<!-- planner-discipline-allow: next/ -->
<!-- planner-discipline-allow: next-auth -->

<task type="auto">
  <name>Task 2: Religar realtime-provider.tsx ao novo módulo e trocar os cinco acessos diretos por chamadas aos wrappers</name>
  <files>features/realtime/model/realtime-provider.tsx</files>
  <action>
Em `features/realtime/model/realtime-provider.tsx`, apague o que foi para
`storage.ts` na Task 1 e troque os cinco acessos diretos por chamadas às funções do
novo módulo. Nada mais no arquivo pode mudar: mesma ordem de hooks, mesmas listas de
dependência de `useEffect`/`useCallback`, mesmos nomes de refs e de estado, mesmo
corpo dos efeitos fora dos trechos descritos abaixo.

**Passo 1 — remoções.** Tire do arquivo:
- as duas constantes de storage do topo (prefixo da chave e versão do payload);
- a constante de teto de ids de evento memorizados (ela volta como import — ver
  Passo 2);
- o tipo local do payload persistido;
- a função local que monta a chave;
- o parser local tolerante.

Mantenha intocadas todas as outras constantes do topo: teto de tamanho de mensagem,
intervalo de poll de status, timeout de abertura de socket, timeout de handshake de
replay, margem de abertura e TTL de fallback de ticket, e a janela de estabilidade
da reconexão.

**Passo 2 — import novo.** Acrescente um import de `./storage` (relativo,
ao lado dos imports de `./types`, `./protocol` e `../api/realtime-api` já
existentes, na mesma formatação multi-linha do arquivo) trazendo exatamente:
- valores: a constante de teto de ids de evento memorizados,
  `clearPersistedStateStorage`, `loadPersistedState`, `savePersistedState`;
- tipo: o tipo do payload persistido.

**Passo 3 — enxugar o import de `./protocol`.** Remova de lá os dois símbolos que
ficaram sem consumidor no provider: o limite de tamanho de id de evento e o parser
de mapa de tópicos (ver `<discovery_notes>` item 5). **Não remova** o predicado de
record nem a coerção de sequência — os dois continuam em uso no `onmessage`. O
restante do import (heartbeat padrão, watchdog, os dois classificadores de
autorização, parser de envelope, parser de status, backoff e o tipo do instantâneo
de status) fica como está.

**Passo 4 — os cinco pontos de acesso.** Reescreva cada um exatamente assim,
preservando gatilho, ordem e comentário:

- **Ponto A — callback `clearPersistedState`.** Mantenha o `if` combinado como
  está hoje (existência do usuário ativo **e** o guard de ambiente — item 2) e
  troque só o miolo protegido pela chamada de remoção do módulo novo, passando a
  identificação do usuário ativo. O comentário que hoje vive dentro do bloco de
  captura — o que diz que o storage pode estar bloqueado por política do navegador e
  que a sessão ainda precisa ser encerrada normalmente — sobe para logo acima da
  chamada, com o mesmo texto. As instruções seguintes do callback (abortar os
  controllers de leitura, limpar o conjunto, baixar a flag de storage pronto,
  resetar o estado do cliente) e a lista de dependências ficam idênticas. **Não
  renomeie este callback** (item 11).

- **Ponto B — efeito de persistência.** A montagem do objeto anotado continua igual,
  inclusive o truncamento da lista de ids pelo teto (agora importado). Troque a
  gravação protegida por uma chamada única de escrita, passando a identificação do
  usuário e o objeto. O comentário sobre o realtime continuar funcional sem
  persistência sobe para logo acima da chamada, com o mesmo texto. O bloco
  `eslint-disable` que precede o efeito, a condição de guarda inicial e a lista de
  dependências não mudam.

- **Ponto C — ramo não-autenticado do efeito de conexão.** Troque a remoção
  protegida por uma chamada de remoção do módulo novo, com o mesmo argumento. O
  comentário que diz que, sem storage, basta limpar o estado em memória sobe para
  logo acima da chamada. As quatro instruções seguintes do ramo (zerar as duas refs
  de usuário/token ativos, zerar a ref de usuário carregado, resetar o estado) e o
  `return` permanecem na mesma ordem.

- **Ponto D — ramo de troca de conta.** Mesma troca, mesmo argumento (a ref de
  usuário ativo, lida **antes** de ela ser sobrescrita logo abaixo — não mude essa
  ordem). O comentário sobre a troca de conta reiniciar todo o estado em memória
  sobe para logo acima da chamada. A instrução seguinte que zera a ref de usuário
  carregado permanece.

- **Ponto E — ramo de carga inicial.** As quatro linhas de hoje (declaração mutável
  inicializada em nulo, bloco protegido com leitura aninhada no parser, e o ramo de
  captura que reatribui nulo) colapsam em **uma** atribuição imutável recebendo o
  resultado da função de leitura do módulo novo. Confirme que a variável não é
  reatribuída depois — não é: os cinco usos seguintes são só leitura. O `if/else`
  posterior (restaurar sequência, revisões, revisões lidas e as duas estruturas de
  ids vistos; ou resetar o estado), a marcação do usuário carregado e a subida da
  flag de storage pronto ficam exatamente como estão.

Ao terminar, o arquivo não pode ter nenhuma referência restante ao storage do
navegador — todo acesso passa pelos três wrappers.

Não altere `realtime-provider.test.tsx` — nenhuma linha (item 7). Ele é a régua
desta tarefa. Se algum teste falhar, o defeito está na extração, não no teste; em
particular, falha nas asserções de chave literal significa que a montagem da chave
ou a forma de acesso ao storage foi alterada (item 8).

Não altere `protocol.ts`, `types.ts`, `realtime-api.ts` nem
`features/realtime/index.ts`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f package.json && npx tsc --noEmit && npx vitest run realtime && test "$(grep -c 'from "./storage"' features/realtime/model/realtime-provider.tsx)" = "1" && test "$(grep -c 'window\.localStorage' features/realtime/model/realtime-provider.tsx)" = "0" && test "$(grep -vE '^\s*(//|\*|/\*)' features/realtime/model/realtime-provider.tsx | grep -cE '^(export )?(function|type|const) (storageKey|parsePersistedState|PersistedRealtimeState|STORAGE_PREFIX|STORAGE_VERSION|MAX_SEEN_EVENT_IDS)')" = "0" && test "$(grep -cE '^\s+(MAX_EVENT_ID_LENGTH|parseTopicMap),' features/realtime/model/realtime-provider.tsx)" = "0" && test "$(grep -cE '^\s+(isRecord|toSequence),' features/realtime/model/realtime-provider.tsx)" = "2" && test "$(grep -c 'RECONNECT_STABLE_MS' features/realtime/model/realtime-provider.tsx)" = "2" && test -z "$(git diff HEAD --name-only -- features/realtime/model/realtime-provider.test.tsx features/realtime/index.ts features/realtime/model/types.ts features/realtime/model/protocol.ts features/realtime/api/realtime-api.ts features/realtime/model/storage.ts)"</automated>
  </verify>
  <done>
`realtime-provider.tsx` tem exatamente um import de `./storage`, zero acessos
diretos ao storage do navegador e zero declarações locais do que migrou. Os dois
símbolos órfãos saíram do import de `./protocol` e o predicado de record e a coerção
de sequência continuam lá. A janela de estabilidade da reconexão e as demais
constantes que ficam seguem no arquivo. `npx tsc --noEmit` limpo e a suíte de
realtime verde, com o arquivo de teste, `protocol.ts`, `types.ts`, `realtime-api.ts`
e `index.ts` sem nenhuma alteração na árvore de trabalho.
  </done>
</task>

<!-- planner-discipline-allow: window.localStorage -->
<!-- planner-discipline-allow: storageKey -->
<!-- planner-discipline-allow: parsePersistedState -->
<!-- planner-discipline-allow: PersistedRealtimeState -->
<!-- planner-discipline-allow: STORAGE_PREFIX -->
<!-- planner-discipline-allow: STORAGE_VERSION -->
<!-- planner-discipline-allow: MAX_SEEN_EVENT_IDS -->
<!-- planner-discipline-allow: MAX_EVENT_ID_LENGTH -->
<!-- planner-discipline-allow: parseTopicMap -->

<task type="auto">
  <name>Task 3: Gates finais do projeto e registro das decisões no SUMMARY</name>
  <files>(nenhum arquivo de código — task de verificação)</files>
  <action>
Rode os gates do projeto inteiro, na raiz do frontend, e feche a tarefa.

1. `npx tsc --noEmit` — precisa passar limpo.
2. `npx eslint features/realtime` — sem erro. Confira também que não sobrou
   **warning** de variável/import não usado nos dois arquivos tocados: o ESLint
   daqui reporta isso como aviso, não como erro, então ele não derruba o comando
   (ver `<discovery_notes>` item 5).
3. `pnpm test` — suíte completa do frontend, como pede a validação desta tarefa. Se
   houver falha, aplique a triagem da **Armadilha de ferramenta 3**: qualquer falha
   fora de `features/realtime/` deve ser re-executada isoladamente com
   `npx vitest run <caminho-do-arquivo>`; se passar isolada, é a instabilidade
   pré-existente sob carga já documentada na task anterior, não regressão desta
   mudança — registre isso no SUMMARY em vez de "consertar" o teste. Falha **dentro**
   de `features/realtime/` é regressão real desta tarefa e deve ser corrigida no
   código de produção, nunca no arquivo de teste.

Depois, escreva o SUMMARY registrando explicitamente as decisões de projeto que este
plano tomou, para que a próxima fatia do mesmo arquivo herde o critério:
- a montagem da chave ficou **privada** e o parser ficou **exportado** — os dois são
  puros, mas expor a montagem reabriria o acoplamento que a tarefa fecha, enquanto o
  parser é o ponto de teste isolado natural;
- o teto de ids de evento memorizados virou export compartilhado (mesmo padrão dos
  dois valores compartilhados por `protocol.ts` na task 260831-kpx);
- o guard de ambiente ficou no provider, no único ponto onde já existia, em vez de
  ser absorvido pelos wrappers — decisão de "nenhuma mudança não pedida", não de
  necessidade técnica;
- os quatro comentários de justificativa da falha tolerada foram preservados nos
  call sites, um a um, porque cada um explica um contexto diferente; o contrato
  genérico de engolir falha ficou documentado no módulo novo.

Registre também, como follow-up **fora** deste plano: `storage.ts` ainda não tem
teste próprio (a cobertura vem indiretamente pela suíte do provider) e
`realtime-provider.tsx` deve terminar esta tarefa com ~985 linhas — a máquina de
estado da conexão segue como candidata natural à próxima fatia.

Não faça push. Os commits ficam em `develop` aguardando aprovação explícita da
desenvolvedora.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f package.json && npx tsc --noEmit && npx eslint features/realtime && npx vitest run realtime && test "$(git hash-object features/realtime/model/realtime-provider.test.tsx)" = "00adf530d57aa2728919338fa46ef8e1009561c2" && test "$(git hash-object features/realtime/index.ts)" = "ba07ec6838902e8371aa23be3986f996bc6d42d3" && test "$(git hash-object features/realtime/model/types.ts)" = "04834de4eb0d133e82a7811c467ea1a293a683f1" && test "$(git hash-object features/realtime/model/protocol.ts)" = "aa48dcf0643f63ae498342c00e121e270b6c7f4b" && test "$(git hash-object features/realtime/api/realtime-api.ts)" = "fb9d33fa13b4e78d3498937907a08402a1918391" && test -z "$(git status --porcelain -- features/realtime)"</automated>
  </verify>
  <done>
Typecheck, lint do feature e suíte de realtime verdes; suíte completa executada e
qualquer falha fora de `features/realtime/` triada e justificada no SUMMARY. Os
cinco arquivos que não podiam mudar têm blob idêntico ao do início da tarefa e não
há nada pendente na árvore dentro de `features/realtime`. SUMMARY escrito com as
quatro decisões e os follow-ups. Nenhum push feito.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| navegador → `window.localStorage` | Conteúdo persistido é gravável pelo próprio usuário (DevTools) e por qualquer script na mesma origem; volta para dentro do app como dado não confiável na próxima carga |
| storage → estado do provider | O cursor restaurado alimenta sequência, revisões e a lista de ids vistos, que governam o replay pedido ao backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-lb9-01 | Tampering | payload persistido lido em `loadPersistedState` | mitigate | A validação existente é preservada byte a byte no parser movido: checagem de versão, coerção de sequência para inteiro seguro não-negativo, coerção dos mapas por tópico e filtragem dos ids por tipo e comprimento máximo. Nenhum campo passa a ser aceito sem validação por causa desta refatoração — gate de blob em `protocol.ts` e suíte do provider inalterada como prova |
| T-lb9-02 | Denial of Service | crescimento indefinido da lista de ids vistos | mitigate | O truncamento pelo teto de 512 ids continua aplicado nos dois lados (na leitura, dentro do parser; na escrita, na montagem do payload) — a constante passa a ser exportada de um único lugar, eliminando o risco de as duas pontas divergirem |
| T-lb9-03 | Information Disclosure | dados de cursor legíveis por script na mesma origem | accept | O payload contém apenas números de sequência/revisão e ids de evento opacos — nenhum dado de pedido, cliente ou credencial. A chave é escopada por usuário e removida no logout e na troca de conta, comportamento preservado nos pontos A, C e D |
| T-lb9-SC | Tampering | instalação de pacotes | accept | Nenhuma dependência nova é adicionada por este plano — os dois arquivos usam apenas API de navegador e módulos já existentes do próprio feature. Não há gate de legitimidade de pacote a executar |

</threat_model>

<verification>
- `npx tsc --noEmit` limpo na raiz do frontend
- `npx eslint features/realtime` sem erro e sem warning novo de símbolo não usado
- `npx vitest run realtime` verde, com `realtime-provider.test.tsx` byte a byte
  idêntico ao estado inicial (blob `00adf53`)
- `pnpm test` executado; falhas fora de `features/realtime/` triadas por re-execução
  isolada contra a instabilidade pré-existente conhecida
- `features/realtime/index.ts`, `types.ts`, `protocol.ts` e `realtime-api.ts` com
  blob inalterado — a superfície pública e as camadas vizinhas não se moveram
- Nenhuma referência a storage do navegador restante em `realtime-provider.tsx`
- Dois commits atômicos em `develop`, sem push
</verification>

<success_criteria>
- `features/realtime/model/storage.ts` existe e concentra chave, formato versionado,
  parser e as três operações de I/O do cursor realtime
- `realtime-provider.tsx` cai de 1070 para ~985 linhas e passa a acessar
  persistência exclusivamente pelos três wrappers
- Os cinco pontos de acesso mantêm gatilho, ordem, tolerância a falha e os quatro
  comentários de justificativa
- A chave gravada continua exatamente a mesma string de hoje
- Zero alteração em arquivo de teste e zero alteração na superfície pública do
  feature
</success_criteria>

<output>
Create `.planning/quick/260831-lb9-extrair-camada-de-storage-persist-ncia-l/260831-lb9-SUMMARY.md` when done
</output>
